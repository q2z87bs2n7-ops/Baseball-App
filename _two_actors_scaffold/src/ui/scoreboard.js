// Scoreboard + history rendering.
//
// STUB — implementation deferred to Phase 6.

import { state } from '../state.js';

export function renderScoreboard() {
  // TODO: render aggregate stats (wins per profile, avg hops, fastest solve, etc.)
  void state;
}

export function renderHistory() {
  // TODO: render list of state.scoreboard.rounds with replay action.
  void state;
}
