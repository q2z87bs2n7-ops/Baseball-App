// Active-round UI. Coordinates the four sub-views:
//   1. setter-pick    (Player A selects actor A and actor B)
//   2. handoff        (pass-to-solver screen)
//   3. solve          (chain-building loop)
//   4. reveal         (win / give-up animation, score recording)
//
// STUB — implementation deferred to Phase 3+.

import { state } from '../state.js';

export function initPlayScreen() {
  // TODO: detect state.currentRound and render the appropriate sub-view.
  void state;
}
