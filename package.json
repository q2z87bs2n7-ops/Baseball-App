{
  "name": "baseball-app",
  "version": "4.20.8",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "dependencies": {
    "web-push": "^3.6.7",
    "@upstash/redis": "^1.34.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.0"
  }
}
